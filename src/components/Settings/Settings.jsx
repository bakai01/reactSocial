import React from "react";

import style from "./Settings.module.css";

const Settings = () => {
    return (
        <div className={style.setting}>
            Это настройки чего-там
        </div>
    );
};

export default Settings;